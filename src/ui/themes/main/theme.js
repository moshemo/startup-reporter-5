import {
  breakpoints,
  borderWidths,
  fonts,
  fontSizes,
  fontWeights,
  lineHeights,
  letterSpacing,
  radii,
  shadows,
  sizes,
  space,
} from 'Shared/variables'

import { colors } from './variables/colors'

const mainTheme = {
  // Common
  borderWidths,
  breakpoints,
  fonts,
  fontSizes,
  fontWeights,
  lineHeights,
  letterSpacing,
  radii,
  shadows,
  sizes,
  space,

  // Colors
  colors,
}

export default mainTheme
